import {style} from '@vanilla-extract/css';

export const card = style({
  maxWidth: '30rem',
  margin: '3rem auto',
  textAlign: 'center',
  padding: '2rem',
});

export const logo = style({
  width: '4rem',
  height: '4rem',
});
